--# (project_name = 'EmojiBloX - funTasia BloX World')
--# (project_alias = 'emojiblox')
--# (file_name = 'init')
--# (file_extension = 'lua')
--# (file_format = 'lua script')
--# (file_version = {major_version = 0, minor_version = 1, patch_version = 0, suffix_version = 'alpha'})
--# [!] File version numbers have to match with that release version number where the file was last amended
--# (author = {surname = 'Christian', lastname = 'Trant'})
--# (date_of_creation = {day = 04, month = 06, year = 2017})
--# (date_of_last_change = {day = 04, month = 06, year = 2017})
--# (license = 'CC0')

emojiblox = {}

emojiblox.name = minetest.get_current_modname()
emojiblox.fullname = 'EmojiBlox - funTasia BloX World'
emojiblox.path = minetest.get_modpath(emojiblox.name)
emojiblox.version = {major = 0,
                    minor = 1,
					patch = 0,
					suffixes = {'unreleased',
            					'alpha'}}

